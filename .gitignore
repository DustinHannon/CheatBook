node_modules
.next
out

# Agent context — local working-tree only, never committed
**/CLAUDE.md

.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.vercel

# QA agent — local credentials + generated run reports
/.claude/qa-agent/credentials.local.json
/qa-reports/

# Scratch
/.tmp/

# Build cache
tsconfig.tsbuildinfo

# QA / Playwright run artifacts
.playwright-mcp/
/F[0-9]*.png
